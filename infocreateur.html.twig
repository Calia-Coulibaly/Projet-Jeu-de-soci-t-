{% extends "page/createur.html.twig" %}
{% block detailcreateur %}
	<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
		<div class="container-fluid">
			<a href="?page=accueil" style="width: 10%;"><img src="img/test.png" style="width: 50%;"></a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarNavAltMarkup">
				<div class="navbar-nav">
					{% if session.acces =="oui" %}
						{# {{dump (session)}} #}
						<a class="nav-link" href="?page=accueil">Accueil</a>
						<a class="nav-link" href="?page=jeux">Jeux</a>
						<a class="nav-link" href="?page=mesjeux">Mes jeux</a>
						<a class="nav-link" href="?page=profil">Profil</a>
						<a class="nav-link" href="?page=deconnect">Deconnexion</a>
					{% else %}
						<a class="nav-link" href="?page=accueil">Accueil</a>
						<a class="nav-link" href="?page=jeux">Jeux</a>
						<a class="nav-link" href="?page=connect">Connexion</a>
					{% endif %}
				</div>
			</div>
		</div>
	</nav>
    <div>
    <div style="margin-left:10%;">
      <p>Pseudo de l'utilisateur : <strong>{{crea.pseudo}}</strong></p>
      <p>Membre depuis le : <strong>{{crea.inscription}}</strong></p>
      {# <button type="button" class="btn btn-outline-primary disabled position-relative">Contact
      <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">1+
      <span class="visually-hidden">unread messages</span>
    </span>
    </button> #}
    </div>
    <div>
    </div>
  </div>
<h3 style="text-align:center;">Découvrez les jeux ajoutés par {{crea.pseudo}} </h3>
  <div class="my-2 mx-2 d-flex flex-row ">
  {% for jeu in jeux %}
  <div class="w-50 d-flex flex-row border border-dark my-2 mx-2">
    <img src={{"./img/"~ jeu.logoJeu}} class="card-img-top" style="width: 60%;" alt="Image du jeu " {{jeu.nomJeu}}>
    <div class="card-body">
      <h5 class="card-title">{{jeu.nomJeu}}</h5>
      <div>
        {#
        <pre>{{dump(jeu)}}</pre> #}

        <a class="btn btn-outline-primary disabled">👤{{jeu.nbr_joueurJeu}}</a>
        <a class="btn btn-outline-primary disabled">⌚️{{jeu.dureeJeu}} min</a>
        <a class="btn btn-outline-primary disabled">{{jeu.nomCat}}</a>
      </div>
      <br>
      <p>{{jeu.date_ajoutJeu}}</p>
      <a href="?page=jeux&nom={{jeu.nomJeu}}" class="btn btn-primary ">Afficher plus</a>
    </div>
  </div>
  {% endfor %}
</div>
 {% endblock %}